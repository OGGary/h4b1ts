# Copy to keystore.properties (which is gitignored) and fill in.
#
# Generate the upload key yourself with keytool, outside the repo. Never commit
# the filled-in file or the .jks itself.
#
# storeFile takes an absolute path, or one relative to the app/ module.

storeFile=C:/Pfad/zu/deinen/Schluesseln/h4b1ts-upload.jks
storePassword=
keyAlias=h4b1ts-upload
keyPassword=
