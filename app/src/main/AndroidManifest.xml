<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Second path for the shield when a ROM drops background activity starts. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Second detector when the accessibility service is off or was killed. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Deliberately no QUERY_ALL_PACKAGES: only launcher apps are queried.
         That covers everything the user can open and avoids the Play Store
         declaration. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.H4b1ts">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.BlockActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity="de.h4b1ts.app.block"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.H4b1ts.Block" />

        <service
            android:name=".block.UsageFallbackService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Detects which app is in the foreground so apps the user has blocked can be blocked" />
        </service>

        <activity
            android:name=".ui.FocusActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="false"
            android:theme="@style/Theme.H4b1ts.Block" />

        <receiver
            android:name=".block.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".block.HealthCheckReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
